{
  "buildCommand": "cd ../.. && node node_modules/prisma/build/index.js generate --schema=apps/provider-portal/prisma/schema.prisma && npm run build -- --filter=provider-portal",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm install",
  "ignoreCommand": "npx turbo-ignore provider-portal",
  "crons": [
    {
      "path": "/api/cron/collect-metrics",
      "schedule": "*/15 * * * *"
    }
  ]
}

